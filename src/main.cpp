#include <Arduino.h>
#define LED 2

// Only runs once
void setup()
{
  pinMode(LED, OUTPUT);
}

// Main loop
void loop()
{
  // digitalWrite(LED, HIGH);
  // delay(500);
  // digitalWrite(LED, LOW);
  // delay(500);
}
